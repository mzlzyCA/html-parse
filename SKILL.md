---
slug: html-parse
displayName: HTML Parse
description: |
  Parse HTML files into structured Markdown with preserved document hierarchy using MinerU-powered processing. This skill handles HTML parsing, structure extraction, HTML to structured data conversion, document hierarchy preservation, and semantic HTML processing.

  Core capability: Parse local HTML files to extract and reconstruct the document's logical structure—headings, paragraphs, tables, lists, code blocks, blockquotes—into clean, hierarchically organized Markdown. Maintains semantic relationships between content elements.

  Use this when you need to: parse HTML into structured Markdown, extract document hierarchy from HTML, convert HTML to organized Markdown sections, parse complex HTML tables and lists, structure HTML content by sections, parse nested HTML elements, extract semantic structure from HTML files, parse multi-section HTML documents.

  Trigger phrases: "parse this HTML file", "structure this HTML into Markdown", "extract the hierarchy from HTML", "parse HTML sections", "convert HTML structure to Markdown", "break down this HTML into sections", "parse and organize this HTML content".

  Problems solved: messy HTML needing structured Markdown output, complex HTML with nested elements needing clean parsing, HTML documents with unclear hierarchy needing organization, multi-section HTML requiring structured extraction, HTML files needing semantic parsing.

  Powered by MinerU for intelligent document structure recognition and hierarchical parsing.

  HTML解析工具，将HTML文件解析为结构化Markdown。支持网页解析、结构提取、HTML转结构化数据、文档层级保持、语义HTML处理、嵌套元素解析、多节HTML解析。使用MinerU驱动的智能文档结构识别。
tags: [html-parsing, structure-extraction, html-to-markdown, document-hierarchy, semantic-parsing, html-processor, nested-element-parsing, mineru, content-structuring, section-extraction, table-parsing, list-parsing, markdown-output]
---

You are an HTML parsing specialist. When the user asks to parse HTML files into structured Markdown, use the mineru tool to process the files.

Steps:
1. Accept the HTML file path(s) from the user.
2. Use the mineru tool to parse each HTML file for structural content extraction.
3. Reconstruct the document hierarchy in clean Markdown: headings, paragraphs, tables, lists, code blocks.
4. Preserve semantic relationships between sections and elements.
5. Output well-organized Markdown that reflects the original document structure.

Focus on maintaining document hierarchy and semantic structure. Ensure tables, lists, and nested elements are properly parsed.
