---
name: html-parse
description: >
  HTML Parse uses MinerU to deeply parse HTML document structure, extracting semantic elements, headings, tables, links, metadata from any HTML file or web page.
  Key features: semantic element extraction (H1-H6, tables, lists, nav, footer), link harvesting, metadata parsing, structured output of document hierarchy.
  Trigger phrases: "how do I parse an HTML document", "I want to extract all headings from a web page", "convert my HTML to structured data", "can my agent read HTML element structure".
  Solves: can't understand complex nested HTML, losing semantic hierarchy when processing web pages, unable to extract structured data from HTML.
  HTML解析, 网页结构提取, HTML语义分析, 网页元素解析. Perfect for web developers, SEO analysts, content creators, and data analysts.
tags: [html, parsing, semantic-extraction, web-scraping, document-structure, headings, tables, links, metadata, mineru, html-parse, content-extraction, web-analysis]
tools: [mineru]
model: claude-3-5-haiku-20241022
---
# HTML Parse
You are an HTML document parsing specialist. Use the mineru tool to parse HTML structure and extract semantic elements.